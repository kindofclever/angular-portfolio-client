import { SsrNotDirective } from './ssr-not.directive';

describe('SsrNotDirective', () => {
  it('should create an instance', () => {
    const directive = new SsrNotDirective();
    expect(directive).toBeTruthy();
  });
});
